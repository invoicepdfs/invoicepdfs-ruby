=begin
#InvoicePDFs

#InvoicePDFs turns a JSON document into a polished PDF — invoices, credit notes, quotes, receipts, proformas, purchase orders and delivery notes. The API validates your data, computes totals correctly (tax, discounts, shipping), and returns a downloadable, professional PDF.  This package is the official client for the InvoicePDFs API, generated from the OpenAPI specification.  Documentation: https://invoicepdfs.com

The version of the OpenAPI document: 0.2.0

Generated by: https://openapi-generator.tech
Generator version: 7.7.0

=end

require 'date'
require 'time'

module InvoicePDFs
  class NumberingSequenceCreateRequest
    attr_accessor :name

    attr_accessor :document_type

    attr_accessor :prefix

    attr_accessor :date_pattern

    attr_accessor :padding

    attr_accessor :next_number

    attr_accessor :reset

    class EnumAttributeValidator
      attr_reader :datatype
      attr_reader :allowable_values

      def initialize(datatype, allowable_values)
        @allowable_values = allowable_values.map do |value|
          case datatype.to_s
          when /Integer/i
            value.to_i
          when /Float/i
            value.to_f
          else
            value
          end
        end
      end

      def valid?(value)
        !value || allowable_values.include?(value)
      end
    end

    # Attribute mapping from ruby-style variable name to JSON key.
    def self.attribute_map
      {
        :'name' => :'name',
        :'document_type' => :'document_type',
        :'prefix' => :'prefix',
        :'date_pattern' => :'date_pattern',
        :'padding' => :'padding',
        :'next_number' => :'next_number',
        :'reset' => :'reset'
      }
    end

    # Returns all the JSON keys this model knows about
    def self.acceptable_attributes
      attribute_map.values
    end

    # Attribute type mapping.
    def self.openapi_types
      {
        :'name' => :'String',
        :'document_type' => :'String',
        :'prefix' => :'String',
        :'date_pattern' => :'String',
        :'padding' => :'Integer',
        :'next_number' => :'Integer',
        :'reset' => :'String'
      }
    end

    # List of attributes with nullable: true
    def self.openapi_nullable
      Set.new([
      ])
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(attributes = {})
      if (!attributes.is_a?(Hash))
        fail ArgumentError, "The input argument (attributes) must be a hash in `InvoicePDFs::NumberingSequenceCreateRequest` initialize method"
      end

      # check to see if the attribute exists and convert string to symbol for hash key
      attributes = attributes.each_with_object({}) { |(k, v), h|
        if (!self.class.attribute_map.key?(k.to_sym))
          fail ArgumentError, "`#{k}` is not a valid attribute in `InvoicePDFs::NumberingSequenceCreateRequest`. Please check the name to make sure it's valid. List of attributes: " + self.class.attribute_map.keys.inspect
        end
        h[k.to_sym] = v
      }

      if attributes.key?(:'name')
        self.name = attributes[:'name']
      else
        self.name = nil
      end

      if attributes.key?(:'document_type')
        self.document_type = attributes[:'document_type']
      else
        self.document_type = 'invoice'
      end

      if attributes.key?(:'prefix')
        self.prefix = attributes[:'prefix']
      else
        self.prefix = 'INV-'
      end

      if attributes.key?(:'date_pattern')
        self.date_pattern = attributes[:'date_pattern']
      else
        self.date_pattern = '{YYYY}-'
      end

      if attributes.key?(:'padding')
        self.padding = attributes[:'padding']
      else
        self.padding = 5
      end

      if attributes.key?(:'next_number')
        self.next_number = attributes[:'next_number']
      else
        self.next_number = 1
      end

      if attributes.key?(:'reset')
        self.reset = attributes[:'reset']
      else
        self.reset = 'yearly'
      end
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      warn '[DEPRECATED] the `list_invalid_properties` method is obsolete'
      invalid_properties = Array.new
      if @name.nil?
        invalid_properties.push('invalid value for "name", name cannot be nil.')
      end

      if !@padding.nil? && @padding > 10
        invalid_properties.push('invalid value for "padding", must be smaller than or equal to 10.')
      end

      if !@padding.nil? && @padding < 1
        invalid_properties.push('invalid value for "padding", must be greater than or equal to 1.')
      end

      if !@next_number.nil? && @next_number < 1
        invalid_properties.push('invalid value for "next_number", must be greater than or equal to 1.')
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      warn '[DEPRECATED] the `valid?` method is obsolete'
      return false if @name.nil?
      document_type_validator = EnumAttributeValidator.new('String', ["invoice", "credit_note"])
      return false unless document_type_validator.valid?(@document_type)
      return false if !@padding.nil? && @padding > 10
      return false if !@padding.nil? && @padding < 1
      return false if !@next_number.nil? && @next_number < 1
      reset_validator = EnumAttributeValidator.new('String', ["never", "yearly", "monthly"])
      return false unless reset_validator.valid?(@reset)
      true
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] document_type Object to be assigned
    def document_type=(document_type)
      validator = EnumAttributeValidator.new('String', ["invoice", "credit_note"])
      unless validator.valid?(document_type)
        fail ArgumentError, "invalid value for \"document_type\", must be one of #{validator.allowable_values}."
      end
      @document_type = document_type
    end

    # Custom attribute writer method with validation
    # @param [Object] padding Value to be assigned
    def padding=(padding)
      if padding.nil?
        fail ArgumentError, 'padding cannot be nil'
      end

      if padding > 10
        fail ArgumentError, 'invalid value for "padding", must be smaller than or equal to 10.'
      end

      if padding < 1
        fail ArgumentError, 'invalid value for "padding", must be greater than or equal to 1.'
      end

      @padding = padding
    end

    # Custom attribute writer method with validation
    # @param [Object] next_number Value to be assigned
    def next_number=(next_number)
      if next_number.nil?
        fail ArgumentError, 'next_number cannot be nil'
      end

      if next_number < 1
        fail ArgumentError, 'invalid value for "next_number", must be greater than or equal to 1.'
      end

      @next_number = next_number
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] reset Object to be assigned
    def reset=(reset)
      validator = EnumAttributeValidator.new('String', ["never", "yearly", "monthly"])
      unless validator.valid?(reset)
        fail ArgumentError, "invalid value for \"reset\", must be one of #{validator.allowable_values}."
      end
      @reset = reset
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(o)
      return true if self.equal?(o)
      self.class == o.class &&
          name == o.name &&
          document_type == o.document_type &&
          prefix == o.prefix &&
          date_pattern == o.date_pattern &&
          padding == o.padding &&
          next_number == o.next_number &&
          reset == o.reset
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(o)
      self == o
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [name, document_type, prefix, date_pattern, padding, next_number, reset].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      attributes = attributes.transform_keys(&:to_sym)
      transformed_hash = {}
      openapi_types.each_pair do |key, type|
        if attributes.key?(attribute_map[key]) && attributes[attribute_map[key]].nil?
          transformed_hash["#{key}"] = nil
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[attribute_map[key]].is_a?(Array)
            transformed_hash["#{key}"] = attributes[attribute_map[key]].map { |v| _deserialize($1, v) }
          end
        elsif !attributes[attribute_map[key]].nil?
          transformed_hash["#{key}"] = _deserialize(type, attributes[attribute_map[key]])
        end
      end
      new(transformed_hash)
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def self._deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        {}.tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = InvoicePDFs.const_get(type)
        klass.respond_to?(:openapi_any_of) || klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_hash.to_s
    end

    # to_body is an alias to to_hash (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_hash
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_hash
      hash = {}
      self.class.attribute_map.each_pair do |attr, param|
        value = self.send(attr)
        if value.nil?
          is_nullable = self.class.openapi_nullable.include?(attr)
          next if !is_nullable || (is_nullable && !instance_variable_defined?(:"@#{attr}"))
        end

        hash[param] = _to_hash(value)
      end
      hash
    end

    # Outputs non-array value in the form of hash
    # For object, use to_hash. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    def _to_hash(value)
      if value.is_a?(Array)
        value.compact.map { |v| _to_hash(v) }
      elsif value.is_a?(Hash)
        {}.tap do |hash|
          value.each { |k, v| hash[k] = _to_hash(v) }
        end
      elsif value.respond_to? :to_hash
        value.to_hash
      else
        value
      end
    end

  end

end
